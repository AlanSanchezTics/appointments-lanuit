# Legacy Booking Components

These components are kept for transitional compatibility and are not part of the active booking flow.

- `booking-form.tsx` (uses deprecated `POST /api/reservar`)
- `day-selector.tsx`
- `time-slot-selector.tsx`

Do not use these for new booking work.
